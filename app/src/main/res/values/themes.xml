<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme for the Compose app shell.
         Compose renders its own Material 3 surface; this only controls the
         window background / status bar before/around Compose. -->
    <style name="Theme.MedicationReminder" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Full-screen alarm theme: dark, immersive, no chrome. -->
    <style name="Theme.MedicationReminder.Alarm" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowShowWallpaper">false</item>
    </style>
</resources>
